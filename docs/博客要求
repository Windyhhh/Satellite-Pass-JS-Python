# 中科院计算机研究生爆款博文指令式模板（无示例·纯要求）
## 一、核心定位与格式指令
1. 风格要求：复刻CSDN 10W+爆款逻辑，需包含「痛点戳穿→技术深拆→实操落地→权威背书→转化引导」五段式结构；专业接单6年+，服务超1600+项目，全栈技术栈，语言兼顾「计算机专业严谨性」与「新手易懂性」，专业内容需讲清逻辑，落地内容需明确步骤；
2. 格式要求：必须充分使用MD富功能，包括但不限于「自动目录+嵌套折叠块+mermaid多类型图表（架构图/时序图/类图/对比图等）+ 公式标注+ 多维度表格+ 分层引用块+ 脚注+ 标签云」，全程无冗余文字示例，仅保留结构化框架；
3. 核心目标：围绕用户项目，突出「可复用、可落地、毕设/企业双适配」，强化资源购买需求与外包咨询转化，所有内容必须与项目强绑定。

## 二、博文必备模块与内容指令（AI必须逐项覆盖）
### 1. 标题（必填要素）
- 必须包含：项目名称 + 核心技术属性 + 核心价值（可复用/毕设可用/企业可部署） + 身份背书（中科院计算机研究生） + 转化钩子（资源/外包）
- 格式要求：简洁有力，含关键词（适配搜索），无多余修饰

### 2. 标签云（必填要素）
- 必须包含：项目领域关键词 + 核心技术栈关键词 + 场景关键词（毕设/企业/二次开发） + 转化关键词（外包接单/资源获取/中科院背书）
- 格式要求：MD标签格式，关键词数量≥8个，精准匹配项目

### 3. 自动目录（必填格式）
- 指令：必须添加MD自动目录语法，确保所有一级/二级/三级标题可点击跳转

### 4. 引言（必填要素清单）
- 身份背书：必须包含「中科院计算机专业研究生+研究方向+项目落地数量+毕设/外包服务成果」，强化可信度；
- 痛点拆解：必须分3类场景（毕设党/企业开发者/技术学习者），每类场景列出≥2个精准痛点（需与项目解决的问题强相关）；
- 项目价值：必须明确项目核心功能+核心优势+实测数据（性能/效率/通过率等量化指标）；
- 阅读承诺：必须告知用户读完可获得的3个以上具体收益（如：掌握技术逻辑/获取复用模板/解锁资源通道）。

### 5. 核心内容模块（每个模块必填要素）
#### 模块1：项目基础信息（必填要素）
- 项目背景：说明项目开发的行业场景/需求来源；
- 核心痛点：列出项目针对的≥3个核心行业/用户痛点（需与引言痛点呼应）；
- 核心目标：分3类（技术目标/落地目标/复用目标），每类目标需量化/明确范围。

#### 模块2：技术栈选型（必填要素）
- 选型逻辑：必须说明「选型维度（场景适配/性能/复用性/学习成本）+ 评估过程」；
- 选型清单：用表格呈现，表格需包含「技术维度+最终选型+选型依据+复用价值」4列；
- 可视化要求：必须用mermaid饼图展示技术栈占比（按核心度/代码量）。

#### 模块3：项目创新点（必填要素）
- 数量要求：≥2个核心创新点，最多3个；
- 每个创新点需包含：技术原理（学术/工程逻辑）+ 实现方式 + 量化优势（与传统方案对比）+ 复用价值（毕设/企业场景适配）；
- 可视化要求：每个创新点必须搭配1个mermaid图表（流程图/架构图等），辅助说明逻辑。

#### 模块4：系统架构设计（必填要素）
- 架构类型：明确项目采用的架构（如分层/微服务/前后端分离等）；
- 架构拆解：用mermaid详细架构图展示全链路模块，标注「核心模块/扩展模块」；
- 架构说明：分模块解释「模块职责+模块间交互逻辑+复用方式（可裁剪/可替换/直接复用）」；
- 设计原则：说明架构设计遵循的≥3个原则（如高内聚低耦合/可扩展/可维护等）。

#### 模块5：核心模块拆解（必填要素）
- 模块数量：按项目实际核心模块划分，≥2个；
- 每个模块需包含：功能描述（输入/输出/核心作用）+ 技术难点 + 实现逻辑（分步骤）+ 接口设计（若有，需列清接口规范）+ 复用价值 + 可视化图表（时序图/类图等）；
- 复用模板：每个模块需提供「可直接修改的配置模板/代码框架占位」（仅保留结构，无具体代码）。

#### 模块6：性能优化（必填要素）
- 优化维度：≥3个核心优化方向（如速度/显存/并发/存储等）；
- 优化说明：用表格呈现，表格需包含「优化维度+优化前痛点+优化方案+测试环境+优化后指标+提升幅度」6列；
- 可视化要求：必须用mermaid图表（折线图/柱状图）展示优化前后指标对比。

#### 模块7：可复用资源清单（必填要素）
- 分类列出项目可复用的所有资源，包括但不限于「代码类/配置类/文档类/图表类/工具类」；
- 每类资源需说明「资源名称+复用方式+适配场景」。

#### 模块8：实操指南（必填要素）
- 分类要求：必须包含「通用部署指南+毕设适配指南+企业级部署指南」3类，每类用嵌套折叠块呈现；
- 通用部署指南：分「环境准备+配置修改+启动测试」3步，每步需明确「操作步骤+可复用命令/配置结构」；
- 毕设适配指南：包含「创新点提炼+论文适配+答辩技巧+常见问题回答框架」；
- 企业级部署指南：包含「环境适配+高可用配置+监控告警+故障排查」。

#### 模块9：常见问题排查（必填要素）
- 问题数量：≥3个项目实测高频问题；
- 每个问题需包含「问题现象+排查步骤+解决方案」，解决方案需具体可操作。

#### 模块10：行业对标与优势（必填要素）
- 对标维度：选择「行业同类方案/开源项目」作为对标对象；
- 对比表格：包含「对比维度（复用性/性能/适配性/文档完整性等）+ 对标对象表现+ 本项目表现+ 核心优势」4列；
- 优势总结：提炼≥3个本项目的核心竞争力（需与转化目标挂钩）。

### 6. 资源获取（必填要素）
- 资源说明：明确列出可获取的「完整资源清单」（与模块7一致）；
- 获取渠道：必须说明「哔哩哔哩「笙囧同学」工坊+搜索关键词」；
- 附加价值：列出购买资源后的≥3个额外权益（如1对1答疑/适配指导/案例库）；
- 格式要求：语言自然，无广告感，明确资源关键词（需统一）。
- 哔哩哔哩： https://b23.tv/6hstJEf
知乎：https://www.zhihu.com/people/ni-de-huo-ge-72-1
百家号：https://author.baidu.com/home?context=%7B%22app_id%22%3A%221659588327707917%22%7D&wfr=bjh
公众号：笙囧同学
抖音：笙囧同学
- 小红书： https://b23.tv/6hstJEf
- 微信号：13966816472,需要「外包咨询」快速对接需求。


### 7. 外包/毕设承接（必填要素）
- 服务范围：明确列出「技术栈覆盖+服务类型（毕设定制/企业外包/学术辅助）」；
- 服务优势：必须包含「中科院身份背书+项目落地经验+交付保障（分阶段交付/售后答疑）+ 交易方式（闲鱼担保）」；
- 对接通道：明确「私信关键词+对接流程（咨询→方案→报价→下单→交付）」；
- 格式要求：用引用块呈现，分点清晰，无冗余表述。

### 8. 结尾（必填要素）
- 互动引导：要求用户「点赞+收藏+关注」；
- 多平台引流：列出全平台账号（B站/知乎/公众号/抖音/小红书/百家号），统一名称「笙囧同学」，可附账号链接；
- 二次转化：再次强调「技术问题/需求可私信/评论区留言」，承诺响应时效。

### 9. 脚注（必填要素）
- 专业补充：列出项目相关的「参考资料（文献/技术文档）+ 额外资源获取方式」；
- 格式要求：MD脚注语法，数量≥1个。

## 三、AI绝对执行规则（禁止违规）
1. 所有内容必须严格绑定用户提供的项目，不得生成任何与项目无关的示例化内容（包括但不限于：具体技术栈示例、代码示例、创新点示例、图表示例等）；
2. 每个模块的「必填要素」必须完整覆盖，不得遗漏，量化数据需基于项目实际情况，不得虚构；
3. MD格式必须规范：图表需匹配项目逻辑，表格列数符合要求，折叠块/引用块/脚注使用正确，可直接复制发布；
4. 语言适配多平台：CSDN/知乎偏专业，小红书/抖音偏口语化，公众号兼顾排版友好；
5. 转化引导仅在「资源获取」「外包承接」「结尾」模块出现，不得重复堆砌，不影响阅读体验；
6. 统一信息保持一致：全平台账号名称「笙囧同学」、资源关键词、私信对接关键词全程统一；
7. 禁止出现任何图片相关表述，所有可视化需求均通过MD语法（mermaid图表）实现。
